🚀 **Развертывание нескольких сайтов MyST Markdown с помощью Podman и Traefik**

Управление несколькими сайтами с документацией не обязательно означает запуск множества серверных процессов. 

Подготовлена инструкция (https://book.soviar.ru/readme-3/), в которой описан высокоэффективный конвейер развертывания веб-сайтов на базе **MyST Markdown** с использованием архитектуры одного пода (Pod), запущенного как сервис systemd (скрипты разворачивания таких приложений будут выложены в ближайшее время).

✍️ **Что такое MyST Markdown?**

**Markedly Structured Text** — это расширенная версия языка разметки Markdown, специально разработанная для создания технической документации и научных публикаций. 

Сайт на базе MyST - это полноценное статическое веб-приложение, которое собирается из ваших рабочих материалов и доступно, как обычный сайт. Именно так запущен мой сайт https://book.soviar.ru/.

🏗️ Преимущество мультисайтовой конфигурации

Используя **маршрутизацию на основе портов** внутри одного экземпляра Nginx, вы можете размещать несколько независимых репозиториев (например, Сайт А и Сайт Б) в одном поде Podman. Это значительно снижает нагрузку на процессор и память, сохраняя при этом порядок в системе.

🛠️ Ключевые компоненты конвейера:

✅ **MyST Markdown:** Превращайте ваши файлы `.md` или `.ipynb` в профессиональную документацию.

✅ **GitHub Actions:** Автоматизируйте процесс сборки и синхронизации через `rsync` по SSH.

✅ **Rootless Podman:** Запускайте сервисы безопасно как фоновые службы **systemd**.

✅ **Traefik:** Обеспечьте обработку SSL/TLS и маршрутизацию трафика с ваших доменов на нужные внутренние порты.

⚠️ **Внимание!** Дополнительно необходимо прописать правила, по которым движок MyST будет рендерить ваши файлы, например, какие директории исключить. В моем случае мне потребовалось запретить рендеринг md файлов в пользу Юпитеровских ноутбуков, так как теперь у меня каждый ноутбук имеет пару в виде md файла, для чего это нужно, расскажу в другом посте. Если не проставить такие фильтры, то статьи будут публиковаться с дублями - для MyST каждый член пары это отдельная сущность, он рендерит каждую.

📝 Основные моменты настройки:

1. **Общий Nginx:** Используйте независимые блоки `server`, прослушивающие уникальные внутренние порты (например, 80 для Сайта А и 81 для Сайта Б) и читающие различные директории для каждого сайта.
2. **Манифест пода:** Определите несколько томов `hostPath` и сопоставьте их с конкретными значениями `containerPort`.
3. **Динамическая маршрутизация Traefik:** Просто добавьте новый роутер и сервис для каждого домена, указывая на соответствующие порты хоста.

Такая архитектура позволяет добавлять новый сайт с документацией гораздо проще и быстрее. Вы можете за 20-30 минут запустить новый сайт для дополнительного репозитория. Инструкция воспроизводимая, мой второй сайт запущен и работает, но он используется для внутреннего потребления, поэтому ссылку не выкладываю.

#MyST
