https://book.soviar.ru/check-broken-links-py-script/
**Поддержание высокого качества документации — это путь, требующий точности.**

Я уже писал о скрипте `check_broken_links.py` https://t.me/build_to_understand/300, который проверяет файлы на битые кросс-ссылки внутри репозитория. Этот скрипт доработан (переименован!) и внедрен в CI процесс репозитория, это помогает снять когнитивную нагрузку с разработчиков, которым в ином случае нужно постоянно помнить о проверке ссылок перед коммитом.

Как устроен процесс валидации и какая роль скрипта в нем.

🔄 **Трехуровневый процесс валидации**

Используется стратегия «эшелонированной обороны», чтобы отлавливать ошибки до того, как они попадут к пользователю:

1. **Локальные Pre-commit хуки**: Скрипт запускается автоматически на машине разработчика. Он проверяет только измененные файлы (staged files), обеспечивая быструю обратную связь, не прерывая творческий процесс.

2. **GitHub Actions (CI)**: При каждом пуше или пулл-реквесте проводится строгая проверка. Используется дифференциальное сканирование только измененных файлов, то есть пришедших с коммитов, что позволяет не перекладывать ответственность за старые ошибки в репозитории на авторов новых правок.

3. **Ручной аудит**: Для глубокой очистки или после рефакторинга можно вручную запустить полное сканирование репозитория, чтобы поддерживать базу «нулевого технического долга».

💡 **Почему приоритет у файлов `.ipynb` (Jupyter Notebooks)**

Документация в репозитории сознательно ведется в Jupyter-ноутбуках. Этот формат, в отличие от стандартного Markdown, обеспечивает выполнение несколько важных функций:

- ноутбуки позволяют не только писать текст, но и запускать код, что делает тексты живыми,

- юпитеровские ноутбуки - это нативная среда разработки инженеров машинного обучения, в них проводят эксперименты, публикуют результаты, это естественный формат для тех, кто работает с ИИ,

- работа в JupyterLab позволяет рендерить Markdown и MyST https://t.me/build_to_understand/308 тут же, не отходя от кассы, и в этой лаборатории проще также работать именно в ноутбуках.

По этим причинам скрипт оптимизирован для парсинга именно ноутбуков (хотя вы можете проверять файлы любого типа, такая опция реализована).

🛠️ **Ключевые возможности**

✅ **Нулевые зависимости**: Скрипт использует только стандартные библиотеки Python, что обеспечивает максимальную переносимость.

✅ **Интеграция с Git**: Он автоматически определяет корень проекта, что позволяет корректно обрабатывать ссылки относительно корня репозитория (например, `/docs/images/logo.png`).

✅ **Умная фильтрация**: Скрипт игнорирует внешние URL-адреса, почтовые ссылки (`mailto:`) и внутренние якоря, фокусируясь исключительно на целостности файловой системы.

Также можно настроить исключения для проверки как в специальном конфиге `paths.py`, так и непосредственно при запуске скрипта в терминале.

🛡 К скрипту прилагается набор авто-тестов, которые валидируют сам скрипт как в хуке, так и на сервере, если скрипт был изменен. Если тесты были изменены, также производится запуск тестов, чтобы враг точно не прошел.

Потихоньку идем все глубже в девопс. Не ИИ единым жив наш репо.
