**Методология Script Suite: как не дать документации устареть**

💡 **Проблема**

Знакомая ситуация: скрипт обновили, а документация осталась прежней. Через месяц новый разработчик читает устаревшие инструкции и тратит часы на отладку. Documentation drift — бич любого проекта.

🧠 **Идея**

Каждый скрипт в проекте должен иметь полный "suite" — комплект из трёх файлов с единым именованием:
- Скрипт: `tools/scripts/<name>.py`
- Тесты: `tools/tests/test_<name>.py`
- Документация: `tools/docs/scripts_instructions/<name>_py_script.md`

Если меняешь скрипт или тесты — обязан обновить документацию. Автоматически.

⚙️ **Решение**

Написал `check_script_suite.py` (~150 строк), который:
1. Проверяет naming convention при каждом коммите
2. Требует staging документации вместе со скриптом/тестами
3. Отслеживает переименования — если переименовал doc, обнови конфиги

✅ **Что получаем:**
- Документация всегда актуальна
- Нет "сиротских" скриптов без тестов
- Pre-commit блокирует коммит до исправления
- CI/CD валидирует на уровне PR

```bash
# Проверка naming convention
uv run tools/scripts/check_script_suite.py --verbose

# Вывод
OK: check_broken_links has script, test, and doc
OK: prepare_prompt has script, test, and doc
ERROR: Missing test: tools/tests/test_new_script.py
```

```yaml
# .pre-commit-config.yaml
- id: check-script-suite
  name: Check Script Suite
  entry: uv run --active tools/scripts/check_script_suite.py
  files: ^tools/(scripts/.*\.py|tests/test_.*\.py)$
```

Простое правило: **изменил код → обнови документацию**. Теперь это не договорённость, а enforcement.

#Python #DevOps #Documentation #PreCommit #ai_engineering_handbook
