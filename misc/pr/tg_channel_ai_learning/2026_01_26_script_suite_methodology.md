**Методология Script Suite: как не дать документации устареть**

💡 **Проблема**

До боли знакомая итуация: скрипт обновили, а документацию к нему нет. Через месяц новый разработчик (или ты сам) читает устаревшие инструкции и тратит часы на отладку. 

А все потому, что отношение к документации среди среднестатистических разработчиков по остаточному принципу - необязательная работа, на которую можно и забить. Хотя на самом деле такое отношение формирует полноценный технический долг. Более того, в эпоху ИИ такое отношение становится угрозой для компании - документация становится исходным кодом, а сам код - лишь артефактом. Нет хорошего контекста - забудь про качественные артефакты.

🧠 **Идея**

Документация и ее артефакты в виде кода дожны рассматриваться как единая, атомарная сущность, и обрабатываться при версионировании, как транзакция в реляционных базах данных - в коммит проходят либо все, либо никто.

Что реализовал я в своем ai_engineering_handbook репозитории. Каждый скрипт в проекте должен иметь полный "suite" — комплект из трёх файлов с единым именованием:
- Скрипт: `tools/scripts/<name>.py`
- Тесты: `tools/tests/test_<name>.py`
- Документация: `tools/docs/scripts_instructions/<name>_py_script.md`

Если меняешь скрипт или тесты — обязан обновить и документацию. Забыл обновить документацию - гит тебе напомнит.

⚙️ **Решение**

Написал `check_script_suite.py`, который:
1. Проверяет соответствие принятому именованию файлов при каждом коммите,
2. Требует документации вместе со скриптом/тестами в индексе гит (`git add`) - обновил скрипт или тесты, внеси изменение в документацию,
3. Отслеживает переименования — если переименовал документацию, переименуй и файлы с кодом.

✅ **Что получаем:**

- Разработчки не может забыть про документацию, следовательно, она всегда будет актуальна
- Нет "сиротских" скриптов без файла с документацией
- Если коммитишь скрипт без документации, pre-commit и CI блокируют коммит до исправления.

```bash
# Проверка naming convention
uv run tools/scripts/check_script_suite.py --verbose

# Вывод
OK: check_broken_links has script, test, and doc
OK: prepare_prompt has script, test, and doc
ERROR: Missing test: tools/tests/test_new_script.py
```

```yaml
# .pre-commit-config.yaml
- id: check-script-suite
  name: Check Script Suite
  entry: uv run --active tools/scripts/check_script_suite.py
  files: ^tools/(scripts/.*\.py|tests/test_.*\.py)$
```

Простое правило: **изменил код → обнови документацию**. Теперь это не договорённость, а часть процесса.

#Python #DevOps #Documentation #PreCommit #ai_engineering_handbook
