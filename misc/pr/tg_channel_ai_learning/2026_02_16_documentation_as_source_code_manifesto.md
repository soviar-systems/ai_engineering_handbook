**Документация — это исходный код для ИИ**

Принцип зафиксирован в документации проекта: `architecture/manifesto.md`. Идея озвучивалась не раз, теперь она задокументирована официально.

💡 Шон Гроув из OpenAI в докладе «The New Code» (https://www.youtube.com/watch?v=8rABwKRsec4) формулирует суть: «code is a lossy projection from the specification» — код лишь неполное отражение спецификации. Выбросить спецификацию и оставить только сгенерированный код — всё равно что уничтожить исходники и версионировать бинарник.

В эпоху, когда ИИ-системы — RAG-пайплайны, агенты, ассистенты — потребляют документацию как основной вход, это утверждение перестаёт быть метафорой. Устаревшие документы порождают галлюцинации. Метаданные без структуры делают поиск слепым. Отсутствие жизненного цикла ведёт к бесконечному накоплению шума.

Плохая документация — это не неудобство для людей. Это **баг в AI-системе**.

🧠 **Структура репозитория = архитектура кода**

Каждый документ имеет определённое место, отслеживаемые ссылки и проверяемые метаданные. Реструктуризация репозитория — это рефакторинг, который требует обоснования и тестирования, а не произвольное перетаскивание файлов.

Это прямая противоположность базам знаний до-AI эпохи — коллекциям статей без структуры, без связей между документами и без автоматической верификации. Такие коллекции были достаточны, когда их читали только люди. Они непригодны, когда от них зависят AI-системы.

⚙️ **Как это реализовано**

✅ Каждый ноутбук (.ipynb) связан с .md-файлом через Jupytext — именно .md читает ИИ: чистый текст, чистые диффы, без бинарного JSON
✅ Метаданные в YAML-заголовках делают каждый документ машиночитаемым — это контракт для ИИ-потребителей
✅ Устаревшие статьи удаляются из репозитория, а не копятся — иначе RAG будет выдавать неактуальную информацию наравне с актуальной
✅ Архитектурные решения (ADR) фиксируются до реализации — они основа всей разработки
✅ CI/CD проверяет целостность документации перед публикацией

#documentation_as_code #ai_engineering_book #manifesto
