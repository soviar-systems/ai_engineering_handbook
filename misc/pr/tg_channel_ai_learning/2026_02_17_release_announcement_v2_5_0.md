https://soviar-systems.github.io/ai_engineering_handbook/
📢 **Релиз AI Engineering Handbook v2.5.0 — «The Self-Documenting System»**

В v2.4.0 мы навели порядок в архитектурных решениях — ADR получили машиночитаемые метаданные, валидацию, жизненный цикл. Но оставался слепой участок: сами коммиты. Каждый коммит — это микрорешение, и если эти решения не структурированы, то CHANGELOG приходится писать вручную, вспоминая, что и зачем менялось. Это ненадёжно и не масштабируется.

v2.5.0 закрывает этот пробел: система теперь **сама валидирует свои коммиты, сама генерирует CHANGELOG и сама устанавливает хуки** при настройке репозитория. Естественная эволюция от «управляемой архитектуры» к «самодокументирующейся системе».

📝 **Коммит как единица документации**

Ключевой сдвиг в мышлении: коммит — это не просто запись в git-историю, а **структурированный документ**, из которого можно автоматически извлечь CHANGELOG. Хук валидирует формат при каждом `git commit`, скрипт-генератор трансформирует структурированную историю в иерархический CHANGELOG. Конфигурация — в `pyproject.toml`, единый источник правды.

Это последний релиз, для которого release notes написаны вручную. Начиная с v2.6.0, структурированная git-история станет основой для генерации — инфраструктура для этого уже заложена.

🏗 **Когнитивные роли вместо названий инструментов**

Одно из самых важных решений этого цикла — отвязка архитектуры от конкретных инструментов. Старые ADR (26004–26008) были привязаны к Aider, Gemini Flash, конкретным версиям моделей. Каждая смена инструмента требовала правки архитектурного решения — это неправильно.

Новые ADR-26027 и ADR-26028 определяют **когнитивные роли**: reasoning-class модели для синтеза и планирования, agentic-class — для выполнения и структурирования. Неважно, какой именно инструмент используется — важно, какую когнитивную функцию он выполняет. Инструменты меняются — когнитивная модель остаётся.

Фреймворк `aidx` переписан как tool-agnostic Multi-Phase AI Pipeline. Документация очищена от упоминаний конкретных инструментов.

🧹 **Экосистема: консолидация и зрелость**

Параллельно — большая уборка:

- Исследовательские проекты извлечены в монорепо research — в хабе остаются только дистиллированные выводы, а не черновики экспериментов
- Формализован механизм продвижения архитектурных решений. ADR в нашей системе проходят через два этапа: сначала решение предлагается как черновик (proposed) — по сути, это RFC, открытый для обсуждения и доработки. Когда решение проверено практикой и подтверждено, оно продвигается в статус accepted — становится обязательным стандартом. Теперь это продвижение контролируется скриптом: решение не может быть принято, пока у него не заполнены все обязательные секции, не указаны участники, не проставлена дата принятия. Показательно, что в этом релизе гейт проверил сам себя — ADR, описывающий правила продвижения, прошёл через собственный механизм при переводе в accepted
- 6 архитектурных решений прошли этот путь от предложения до стандарта — экосистема выходит из стадии экспериментов, накопленный опыт фиксируется как обязательства
- `pyproject.toml` закреплён как единый хаб конфигурации инструментов — это фундамент для извлечения новых пакетов экосистемы: общая конфигурация живёт в одном месте, и пакеты вроде vadocs смогут читать её напрямую вместо того, чтобы дублировать настройки у себя

И самое важное концептуально — появился **Documentation-as-Code Manifesto** (`architecture/manifesto.md`). Не новая идея, а формализация того, к чему проект двигался с первого релиза: документация — это исходный код для AI-систем, а не приложение к нему. README полностью переписан вокруг этого принципа.

Подробнее: https://github.com/soviar-systems/ai_engineering_handbook/releases/tag/v2.5.0

#ai_engineering_book #ADR #CHANGELOG #CommitConventions
