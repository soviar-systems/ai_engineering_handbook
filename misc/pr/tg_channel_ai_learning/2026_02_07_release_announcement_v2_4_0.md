https://book.soviar.ru
📢 **Релиз AI Engineering Handbook v2.4.0 — «The Governed Architecture»**

Главная идея релиза — навести порядок в архитектурных решениях проекта. Раньше ADR были просто markdown-файлами, теперь у каждого есть YAML-фронтматтер со статусом, датой и тегами. Это подготовка к тому, чтобы в будущем AI-агенты могли сами находить и фильтровать нужные решения, когда репозиторий будет использован для RAG.

🏛 **Управление**

7 новых ADR (26016–26022) и все существующие записи мигрированы на единый формат с YAML-фронтматтером.

Новый скрипт `check_adr.py` автоматически проверяет форматирование и полноту метаданных до попадания в `main`.

Политика жизненного цикла (ADR-26021): 

- устаревшие статьи удаляются из репозитория, чтобы не сбивать с толку ИИ при семантическом поиске по RAG — git-история служит архивом,

- ADR сохраняются все, так как это история принятия решений, актуальность решений ИИ и люди контролируют через метаданные (версия, дата, владелец) и индекс ADR, который разбит на три раздела: актуальные, предложены для реализации, отмененные.

🔧 **Экосистема**

**vadocs** — принято решение, что скрипты валидации документов будут выделены в отдельный движок.

Это позволит упростить конфигурирование и поддержку этого механизма в данном конкретном репо (одна зависимость вместо целого списка зависимостей и системы скриптов в отдельной директории) и, что более важно, даст возможность управлять валидацией документов в любом репо. 

PoC vadocs проведен успешно, пакет извлечён в отдельный репозиторий https://github.com/lefthand67/vadocs/tree/main. Следующие шаги - выстроить механизм управления пакетом и начать переносить в пакет функционал скриптов, которые уже разработаны и используются для валидации текущего репо.

ADR-26020 описывает **Hub-Spoke модель**: репозиторий ai_engineering_book— это хаб стандартов, а извлечённые из него пакеты вроде vadocs — это самостоятельные инструменты экосистемы вокруг концепции "документация-как-код".

Также принято решение о переносе хостинга документации моих публичных репо с личного сервера на GitHub Pages (ADR-26022) - open source проект, пусть и пока мало кому известный, не должен зависеть от личного сервера.

Подробнее: https://github.com/lefthand67/ai_engineering_handbook/releases/tag/v2.4.0

Всю работу веду в Claude Code и чате Gemini, иногда для коммитов использую aider с облачной моделью gemma 27B. 

Работу над пакетом vadocs ведет Claude под моим руководством. PoC я проверял лично, в ручном режиме, файл с итогами здесь: https://github.com/lefthand67/vadocs/blob/main/docs/rnd/poc.ipynb. Следующий непосредственный шаг - создание подробной документации по функционалу, который реализован в PoC версии, полностью под моим контролем. Это позволит мне удерживать понимание происходящего и не скатиться в обычный вайб-кодинг - я сам уже не пишу код, но **я должен хорошо понимать, что мне пишет Клод или любой другой агент**. 

Задача по созданию экосистемы "документация-как-код" нетривиальна и не может быть отдана на откуп автоматической генерации, требуется постоянно проводить много анализа. И даже при таком подходе у меня накопился технический долг, поэтому сейчас буду сбавлять темпы и выправлять механизм.

#ADR #ai_engineering_book #vadocs
